pre-commit:
  commands:
    treefmt:
      run: git diff --cached --name-only --diff-filter=d -z | xargs -0 -r treefmt
      stage_fixed: true
    local_pre_commit:
      run: if [ -f .githooks/pre-commit ]; then bash .githooks/pre-commit; fi
